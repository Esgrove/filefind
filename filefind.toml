# Example configuration for filefind
# Copy this file to ~/.config/filefind.toml

[daemon]
# Paths to index (drives, directories, or network locations).
#
# Can include:
# - Drive letters (e.g., "C:", "D:") - indexes entire drive using fast NTFS MFT scanning
# - Specific directories (e.g., "C:\\Users", "D:\\Projects") - uses MFT scanning but
#   only stores entries under the specified paths (fast AND selective)
# - Mapped network drives (e.g., "Z:") - MFT scanning is attempted first; if not
#   available (most NAS devices), falls back to directory walking automatically
# - UNC paths (e.g., "\\\\server\\share") - uses directory walking (no drive letter)
#
# If empty or not specified, all available local NTFS drives will be auto-detected.
# Network drives are NOT auto-detected - add them explicitly if needed.
#
# Examples:
# paths = ["C:", "D:"]                                    # Index entire local drives
# paths = ["C:\\Users\\Me\\Documents", "D:\\Projects"]   # Index specific directories
# paths = ["C:", "Z:"]                                   # Local drive + mapped NAS
# paths = ["C:", "\\\\nas\\media"]                       # Local drive + UNC path
# paths = []

# Directories to exclude from indexing
exclude = [
    "C:\\Windows",
    "C:\\$Recycle.Bin",
    "C:\\System Volume Information",
    "C:\\ProgramData\\Microsoft",
]

# Manual path mappings from UNC prefix to mapped drive letter.
# Each entry is a [unc_prefix, drive_letter] pair.
# Used by the CLI to display search results with mapped drive letter paths
# instead of UNC paths. The database always stores the real UNC paths
# so the daemon can access them from an elevated process.
#
# Auto-detection via Win32 API is attempted first for drive letters in
# the configured paths. Manual mappings here take priority over auto-detected ones.
#
# path_mappings = [["\\\\192.168.1.106\\Home", "X"], ["\\\\server\\share", "Z"]]

# File patterns to exclude (glob syntax)
exclude_patterns = ["*.tmp", "~$*", "Thumbs.db", "*.parts"]

# Rescan interval for non-NTFS/network drives in seconds
# scan_interval_seconds = 3600

# Log level: "error", "warn", "info", "debug", "trace"
# log_level = "info"

# Verbose output (typically set via CLI --verbose flag)
# verbose = false

# Database location (default: ~/.local/share/filefind/filefind.db)
# database_path = ""

# Force clean scan (delete existing entries before inserting new ones).
# force_clean_scan = false

[cli]
# Default output format:
#   "list"    - list of paths
#   "grouped" - files grouped by directory (default)
#   "info"    - paths with file size
# format = "grouped"

# Maximum number of results to show (0 = unlimited)
# max_results = 100

# Enable colored output
# color = true

# Case-sensitive search by default
# case_sensitive = false

# Show hidden files in results
# show_hidden = false
